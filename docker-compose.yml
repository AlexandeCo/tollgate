version: '3.8'
services:
  tollgate:
    build: .
    ports:
      - "4243:4243"
      - "4244:4244"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - tollgate-data:/root/.tollgate
volumes:
  tollgate-data:
